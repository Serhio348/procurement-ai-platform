# Этап 58 — скоринг как на 51, фразы все

**Статус:** живой прогон этапа 57 снизил точность. Лишние правки
откатили. На площадку по-прежнему уходит каждая фраза профиля.

---

## 1. Цель

Специалист просил все фразы. Вместе с этим в 57 ушло в скоринг:
профиль работ по **названию**, запрос «монтаж» даже если его нет в
словах, и review без объекта. В выдачу попал любой монтаж.

## 2. Что осталось из 57

- капа 4 фразы нет: objects + все keywords
- каждая фраза дочитывается, лимит 100 сырых строк не обрывает очередь
- 4 страницы на фразу, не 6
- кнопка не открывает карточки (этап 56)

## 3. Что снято

- `reconcileSearchIntentPlan`
- works из имени профиля
- монтаж/пусконаладка как отдельные запросы, если их нет в keywords
- «монтаж без объекта → review» — снова discard, как этап 51

Веса 0–100 не менялись.

## 4. Изменения БД

Нет.

## 5. Критерии приёмки

- [x] все фразы профиля уходят на площадку
- [x] «Монтаж КРУ» без объекта профиля снова discard, не входящие
- [x] `npm run verify`
